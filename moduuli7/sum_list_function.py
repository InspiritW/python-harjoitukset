# ============================================
# Module 7, Exercise 4: Sum of a list (function)
# ============================================
# Goal: a function that takes a list of integers and returns their sum.
#
# Steps:
# 1. Define: def sum_list(numbers):
#    Inside: use a loop (or Python's built-in sum()... but try writing it
#    yourself with a loop first, that's the point of the exercise) to add
#    up every number in the list, then return the total.
# 2. In the main program: create a test list yourself, e.g.
#      my_numbers = [3, 7, 2, 9]
# 3. Call sum_list(my_numbers), store the result, print it.
