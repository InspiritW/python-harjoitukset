# ============================================
# Module 7, Exercise 5: Keep only even numbers (function)
# ============================================
# Goal: a function that takes a list of integers and returns a NEW list
# with all the odd numbers removed (the original list stays untouched).
#
# Steps:
# 1. Define: def remove_odds(numbers):
#    Inside: make a new empty list. Loop through `numbers`, and for each
#    one that's even (number % 2 == 0), append it to the new list.
#    Return the new list.
# 2. In the main program: make a test list, call the function, then
#    print BOTH the original list and the new, filtered list — so you
#    can see the original wasn't changed.
