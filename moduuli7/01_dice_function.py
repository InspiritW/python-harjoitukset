# ============================================
# Module 7, Exercise 1: A dice-rolling function
# ============================================
# Goal: write a FUNCTION that rolls one 6-sided die and returns the result.
# Then write a main program that keeps rolling until it gets a 6.
#
# Steps:
# 1. Import random.
# 2. Define a function with no parameters, e.g.:
#      def roll_dice():
#    Inside it: return a random number between 1 and 6.
#    (return, not print! the function hands the value back to whoever
#    called it — see 00_concept_functions.py if this feels fuzzy.)
# 3. In your main program (below the function): start a while loop.
# 4. Inside the loop: call roll_dice(), store the result in a variable,
#    print it.
# 5. If the result is 6: stop the loop (break, or a while condition).
